name: flutter_litert_example
description: Example app and integration-test host for flutter_litert.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_litert: ^3.5.0
  opencv_dart: ^2.2.1+4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  integration_test:
    sdk: flutter
  device_info_plus: ^11.1.0

dependency_overrides:
  flutter_litert:
    path: ../

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/samples/
