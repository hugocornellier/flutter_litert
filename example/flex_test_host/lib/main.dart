import 'package:flutter/widgets.dart';

class FlexTestHostApp extends StatelessWidget {
  const FlexTestHostApp({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

void main() {
  runApp(const FlexTestHostApp());
}
